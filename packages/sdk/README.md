# packages/sdk

TypeScript SDK: typed client for the Lumio API plus Stellar/Soroban helpers
(building transactions, watching contract events, wallet integration helpers).

Published to npm as `@lumio/sdk` once stable.
